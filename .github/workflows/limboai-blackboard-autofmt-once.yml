name: LimboAI Blackboard Autofmt Once

on:
  push:
    branches:
      - feature/monthly-campaign-foundation
    paths:
      - .github/workflows/limboai-blackboard-autofmt-once.yml
      - game/tests/limboai_policy_adapter_contract_test.gd

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout branch
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
        with:
          ref: feature/monthly-campaign-foundation
          persist-credentials: true
      - name: Format pinned test
        run: |
          python3 -m pip install --disable-pip-version-check --no-input 'gdtoolkit==4.5.0'
          gdformat game/tests/limboai_policy_adapter_contract_test.gd
      - name: Commit formatting
        run: |
          if git diff --quiet -- game/tests/limboai_policy_adapter_contract_test.gd; then
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add game/tests/limboai_policy_adapter_contract_test.gd
          git commit -m 'style(combat): format LimboAI Blackboard contract'
          git push origin HEAD:feature/monthly-campaign-foundation
